<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
	xmlns:p="http://www.springframework.org/schema/p"
	xmlns:aop="http://www.springframework.org/schema/aop"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.1.xsd">

	<bean id="dataSource" class="org.apache.commons.dbcp.BasicDataSource">
		<property name="driverClassName" value="com.mysql.jdbc.Driver">
		</property>
		<property name="url"  
			
			value="jdbc:mysql://127.0.0.1:3306/lqt_default?useUnicode=true&amp;characterEncoding=utf-8">
		</property>
		<property name="username" value="root"></property>
		<property name="password" value="root"></property>
		<!-- <property name="username" value="wzzxk"></property>
		<property name="password" value="XPdtWEU47wepJfzH"></property>-->
	</bean>
	
	<bean id="sessionFactory"
		class="org.springframework.orm.hibernate3.LocalSessionFactoryBean">
		<property name="dataSource">
			<ref bean="dataSource" />
		</property>
		<property name="hibernateProperties">
			<props>
				<prop key="hibernate.dialect">
					org.hibernate.dialect.MySQLDialect
				</prop>
				<prop key="hibernate.show_sql">false</prop>
				<prop key="hibernate.hbm2ddl.auto">update</prop>
				
				<prop key="org.hibernate.envers.audit_table_suffix">_revinfo</prop>
                <!--配置审计表名的前缀，默认为_AUD  -->
                <prop key="org.hibernate.envers.audit_table_prefix"></prop>
                <!--配置审计表中记录版本号的字段名称  -->
                <prop key="org.hibernate.envers.revision_field_name">REV</prop>
                <!--配置审计表中数据操作类型的字段名称，0：新增   1：修改   2：删除  -->
                <prop key="org.hibernate.envers.revision_type_field_name">REVTYPE</prop>
                <!--配置是否支持关联表修改时记录修改记录,默认为true  -->
                <prop key="org.hibernate.envers.revision_on_collection_change">true</prop>
                <!--配置是否不对乐观锁字段修改时记录修改记录，即使用Version字段，默认值为true  -->
                <prop key="org.hibernate.envers.do_not_audit_optimistic_locking_field">true</prop>
                <!--配置是否在删除操作时，记录所有字段还是只记录id值。默认为false    false：只记录id   true：记录全部字段  -->
                <prop key="org.hibernate.envers.store_data_at_delete">true</prop>
                <!--配置审计表的schema，默认为null  -->
                <prop key="org.hibernate.envers.default_schema"></prop>
                <!--配置审计表的catalog，默认为null  -->
                <prop key="org.hibernate.envers.default_catalog"></prop>
				
			</props>
		</property>
	
		<property name="mappingDirectoryLocations">
			<list>
				<value>classpath:/com/hibernate</value>
			</list>
		</property>
	</bean>
	
	<!-- aop测试区代码
	<bean id="aopTest" class="aop.AopTest"></bean>
	<bean id="mainExecute" class="aop.MainExecute"></bean>
	<aop:config>
		<aop:aspect id="deal" ref="mainExecute">
			<aop:pointcut id="target" expression="execution(* aop.AopTest.aopTestFun())"/>
			<aop:before method="aopTestFun" pointcut-ref="target"/>
		</aop:aspect>
	</aop:config>
	 -->	
	<import resource="com/Bean.xml" />
</beans>